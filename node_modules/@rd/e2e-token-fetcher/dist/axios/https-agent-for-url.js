"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.httpsAgentForURL = httpsAgentForURL;
var proxy_utils_1 = require("../utils/proxy-utils");
var https_agents_1 = require("./https-agents");
var loglevel_1 = __importDefault(require("loglevel"));
/**
 * If URL requires a proxy, returns a proxified httpsAgent that handles keep-alive.
 * This seems to be needed for corporate Renault proxy.
 *
 * If no proxy is needed, returns a regular httpsAgent.
 */
function httpsAgentForURL(url) {
    var agent;
    var _a = (0, proxy_utils_1.getUrlProxyFromEnv)(url), msg = _a.msg, proxyUrl = _a.proxyUrl;
    var miniUrl = url.split('?').shift();
    if (proxyUrl) {
        agent = https_agents_1.httpsProxyAgent;
        loglevel_1.default.debug("* Using ".concat(proxyUrl, " as a proxy for URL ").concat(miniUrl, " [").concat(msg, "]"));
    }
    else {
        agent = https_agents_1.httpsAgent;
        loglevel_1.default.debug("* Not using a proxy for URL ".concat(miniUrl, " [").concat(msg, "]"));
    }
    return agent;
}
