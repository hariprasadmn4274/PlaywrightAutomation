"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getReportDescription = getReportDescription;
var fs_1 = __importDefault(require("fs"));
var _1 = require(".");
var config_1 = require("../config");
function getReportDescription() {
    var description = "Test execution generated by xray-connector for Test Plan ".concat((0, config_1.getXrayDefaultTestPlanKey)());
    if ((0, config_1.getGatlingSimulation)()) {
        description += '\n\n';
        description += (0, _1.getGatlingStatus)()
            .assertions.map(function (assertion) { return "Assert ".concat(assertion.message, " - ") + (assertion.status ? 'PASS' : 'FAIL'); })
            .join('\n');
    }
    if ((0, config_1.getGatlingReportUrl)()) {
        description += '\n\n';
        description += 'Please refer to Gatling report for details :\n';
        description += (0, config_1.getGatlingReportUrl)();
    }
    if ((0, config_1.getTestPlanFile)()) {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any,@typescript-eslint/no-unsafe-assignment
        var testPlan = JSON.parse(fs_1.default.readFileSync((0, config_1.getTestPlanFile)(), 'utf8'));
        // eslint-disable-next-line @typescript-eslint/no-unsafe-member-access
        var simulations = testPlan['simulations'] || [];
        description += '\n\n';
        description += simulations
            .map(function (simulation, index) {
            var simulationInfo = '';
            simulationInfo += "---- Simulation ".concat(index, " ---\n");
            simulationInfo += "Tag : ".concat(simulation.tag, "\n");
            simulationInfo += simulation.injectionSteps.map(function (step) {
                var simulationStepInfo = '';
                simulationStepInfo += "Injection Profile : ".concat(step.injectionProfile, "\n");
                simulationStepInfo += "Nb users : ".concat(step.users, "\n");
                simulationStepInfo += "Duration : ".concat(step.duration, "\n");
                return simulationStepInfo;
            });
            return simulationInfo;
        })
            .join('\n\n');
    }
    return description;
}
//# sourceMappingURL=get-report-description.js.map