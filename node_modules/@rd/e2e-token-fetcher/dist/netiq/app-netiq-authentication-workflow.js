"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.appNetIQAuthenticationWorkflow = appNetIQAuthenticationWorkflow;
var axios_1 = require("../axios");
var connect_to_app_1 = require("../common/connect-to-app");
var url_utils_1 = require("../utils/url-utils");
var workflow_utils_1 = require("../utils/workflow-utils");
var fix_acr_values_1 = require("./fix-acr-values");
var get_netiq_env_1 = require("./get-netiq-env");
var netiq_login_1 = require("./netiq-login");
var loglevel_1 = __importDefault(require("loglevel"));
function appNetIQAuthenticationWorkflow(startUrl, username, password, response) {
    return __awaiter(this, void 0, void 0, function () {
        var expectedCookie, netIQEnv, idpAuthzURL, idpLoginURL, redirect_uri, authzUrl, msg;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    loglevel_1.default.info("* Authenticating with '".concat(username, "' for '").concat(startUrl, "' using NetIQ"));
                    if (!!response) return [3 /*break*/, 2];
                    return [4 /*yield*/, (0, connect_to_app_1.connectToApp)(startUrl)];
                case 1:
                    response = _a.sent();
                    _a.label = 2;
                case 2:
                    expectedCookie = 'mod_auth_openidc_state_';
                    if (!(0, workflow_utils_1.isCookieSet)(expectedCookie, response))
                        throw new Error("Expected application to set '".concat(expectedCookie, "' cookie."));
                    netIQEnv = (0, get_netiq_env_1.getNetIQEnv)(response.headers.location);
                    idpAuthzURL = "https://".concat(netIQEnv, "/nidp/oauth/nam/authz");
                    idpLoginURL = "https://".concat(netIQEnv, "/nidp//app/login");
                    if (!(0, workflow_utils_1.isRedirectedTo)(idpAuthzURL, response))
                        throw new Error("Expected application to redirect to ".concat(idpAuthzURL));
                    redirect_uri = (0, url_utils_1.getRedirectUri)(response.headers.location);
                    authzUrl = (0, fix_acr_values_1.fixAcrValues)(response.headers.location);
                    return [4 /*yield*/, (0, axios_1.request)({ method: 'GET', url: authzUrl })];
                case 3:
                    response = _a.sent();
                    expectedCookie = 'JSESSIONID';
                    if (!(0, workflow_utils_1.isCookieSet)(expectedCookie, response))
                        throw new Error("Expected idp authz to set '".concat(expectedCookie, "' cookie."));
                    if (!(0, workflow_utils_1.isRedirectedTo)(idpLoginURL, response))
                        throw new Error("Expected idp authz to redirect to ".concat(idpLoginURL));
                    return [4 /*yield*/, (0, axios_1.request)({ method: 'GET', url: response.headers.location })];
                case 4:
                    // ----------------------------------------
                    // Open idp login page
                    // ----------------------------------------
                    response = _a.sent();
                    if (response.status != 200)
                        throw new Error('Expected idp login page to be successfully displayed.');
                    return [4 /*yield*/, (0, netiq_login_1.netIQLogin)(idpLoginURL, username, password)];
                case 5:
                    // ----------------------------------------
                    // Log in to idp
                    // ----------------------------------------
                    response = _a.sent();
                    if (response.status != 200)
                        throw new Error('Expected idp login to be successful.');
                    expectedCookie = 'JSESSIONID';
                    if (!(0, workflow_utils_1.isCookieSet)(expectedCookie, response)) {
                        msg = "idp did not set '".concat(expectedCookie, "' cookie. This usually means your credentials are not valid.");
                        throw new Error(msg);
                    }
                    // ----------------------------------------
                    // Go back to idp authz
                    // ----------------------------------------
                    authzUrl += '&resourceServer=IdentityProvider';
                    return [4 /*yield*/, (0, axios_1.request)({ method: 'GET', url: authzUrl })];
                case 6:
                    response = _a.sent();
                    expectedCookie = '_PA_SDK_SSO_';
                    if (!(0, workflow_utils_1.isCookieSet)(expectedCookie, response))
                        throw new Error("Expected idp authz to set '".concat(expectedCookie, "' cookie."));
                    if (!(0, workflow_utils_1.isRedirectedTo)(redirect_uri, response))
                        throw new Error("Expected idp authz to redirect to ".concat(redirect_uri));
                    return [4 /*yield*/, (0, axios_1.request)({ method: 'GET', url: response.headers.location })];
                case 7:
                    // ----------------------------------------
                    // Open app oidc
                    // ----------------------------------------
                    response = _a.sent();
                    expectedCookie = 'mod_auth_openidc_state_';
                    if (!(0, workflow_utils_1.isCookieSet)(expectedCookie, response))
                        throw new Error("Expected app oidc to set '".concat(expectedCookie, "' cookie."));
                    expectedCookie = 'mod_auth_openidc_session';
                    if (!(0, workflow_utils_1.isCookieSet)(expectedCookie, response))
                        throw new Error("Expected app oidc to set '".concat(expectedCookie, "' cookie."));
                    if (!(0, workflow_utils_1.isRedirectedTo)(startUrl, response))
                        throw new Error("Expected app oidc to redirect to ".concat(startUrl));
                    return [2 /*return*/];
            }
        });
    });
}
