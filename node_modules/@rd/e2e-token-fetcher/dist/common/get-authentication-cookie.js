"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getAuthenticationCookie = getAuthenticationCookie;
var loglevel_1 = __importDefault(require("loglevel"));
var axios_1 = require("../axios");
var app_netiq_authentication_workflow_1 = require("../netiq/app-netiq-authentication-workflow");
var app_okta_authentication_workflow_1 = require("../okta/app-okta-authentication-workflow");
var cache_utils_1 = require("../utils/cache-utils");
var cookies_utils_1 = require("../utils/cookies-utils");
var log_utils_1 = require("../utils/log-utils");
var url_utils_1 = require("../utils/url-utils");
var connect_to_app_1 = require("./connect-to-app");
require('dotenv').config();
function getAuthenticationCookie(config) {
    return __awaiter(this, void 0, void 0, function () {
        var username, password, appUrl, use_cache, key, cookie, response, msg, idp, _a, msg, msg;
        return __generator(this, function (_b) {
            switch (_b.label) {
                case 0:
                    if (!config.username)
                        throw new Error('Undefined username');
                    if (!config.password)
                        throw new Error('Undefined password');
                    if (!config.appUrl)
                        throw new Error('Undefined appUrl');
                    config.use_cache = config.use_cache || true;
                    username = config.username, password = config.password, appUrl = config.appUrl, use_cache = config.use_cache;
                    (0, log_utils_1.setLogLevel)();
                    key = "".concat(username, "@").concat((0, url_utils_1.urlWithTrailingSlash)(appUrl));
                    cookie = use_cache && (0, cache_utils_1.getCookieFromCache)(key);
                    if (!!cookie) return [3 /*break*/, 8];
                    (0, axios_1.resetCookies)(); // Make sure axios cookie store is empty
                    return [4 /*yield*/, (0, connect_to_app_1.connectToApp)(appUrl)];
                case 1:
                    response = _b.sent();
                    if (response.status != 302 || !response.headers.location) {
                        msg = "Application at ".concat(appUrl, " did not send a redirection.");
                        throw new Error(msg);
                    }
                    idp = response.headers.location.replace('https://', '').split('/')[0];
                    loglevel_1.default.info("* Detected IDP '".concat(idp, "'"));
                    _a = idp;
                    switch (_a) {
                        case 'idp.renault.com': return [3 /*break*/, 2];
                        case 'idp2.renault.com': return [3 /*break*/, 2];
                        case 'sso.renault.com': return [3 /*break*/, 4];
                        case 'sso-uat.renault.com': return [3 /*break*/, 4];
                    }
                    return [3 /*break*/, 6];
                case 2: return [4 /*yield*/, (0, app_netiq_authentication_workflow_1.appNetIQAuthenticationWorkflow)(appUrl, username, password, response)];
                case 3:
                    _b.sent();
                    return [3 /*break*/, 7];
                case 4: return [4 /*yield*/, (0, app_okta_authentication_workflow_1.appOktaAuthenticationWorkflow)(appUrl, username, password, response)];
                case 5:
                    _b.sent();
                    return [3 /*break*/, 7];
                case 6:
                    msg = "Application at ".concat(appUrl, " redirected to unexpected IDP: ").concat(idp, ".");
                    throw new Error(msg);
                case 7:
                    cookie = (0, cookies_utils_1.getCookieFromStore)(appUrl, 'mod_auth_openidc_session');
                    if (!cookie) {
                        msg = "Cound not find cookie 'mod_auth_openidc_session' for '".concat(appUrl, "' in store.");
                        throw new Error(msg);
                    }
                    (0, cache_utils_1.addCookieToCache)(cookie, key);
                    _b.label = 8;
                case 8: return [2 /*return*/, cookie];
            }
        });
    });
}
