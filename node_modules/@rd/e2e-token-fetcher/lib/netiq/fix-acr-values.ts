export function fixAcrValues(url: string) {
  const ACR_VALUES = 'acr_values=secure/name/password/uri';
  url = url.replace(/acr_values=[^&]*/g, ACR_VALUES);
  if (url.indexOf(ACR_VALUES) == -1) url += '&' + ACR_VALUES;
  return url;
}
