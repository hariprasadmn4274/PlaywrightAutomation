#!/usr/bin/env node

require('dotenv').config(); // NOSONAR

// Use require to avoid 'SyntaxError: Cannot use import statement outside a module' errors
const getGatlingReport = require('../lib/cli/get-gatling-report').getGatlingReport; // NOSONAR
const print = require('../lib/utils').print; // NOSONAR

const LINE = '------------------------------------------------------------';

print(LINE);
print('XC_FEATURES_REPORT_DIRECTORY:', process.env.XC_FEATURES_REPORT_DIRECTORY);
print('XC_MERGED_REPORT_DIRECTORY:', process.env.XC_MERGED_REPORT_DIRECTORY);
print('XC_GATLING_SIMULATION:', process.env.XC_GATLING_SIMULATION);
print('XC_GATLING_REPORT_URL:', process.env.XC_GATLING_REPORT_URL);
print(LINE);

getGatlingReport();
