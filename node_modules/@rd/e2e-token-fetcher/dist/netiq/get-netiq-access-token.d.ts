import { Token } from '../types';
export declare function getNetIQAccessToken(config: Partial<{
    username: string;
    password: string;
    client_id: string;
    client_secret: string;
    grant_type: string;
    scope: string;
    idp: string;
    use_cache: boolean;
}>): Promise<Token>;
