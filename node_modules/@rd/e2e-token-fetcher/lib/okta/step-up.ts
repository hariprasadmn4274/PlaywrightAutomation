import { request } from '../axios';

export async function stepUp(stateToken: string, oktaEnv: string) {
  const stepUpUrl = `https://${oktaEnv}/login/step-up/redirect?stateToken=${stateToken}`;
  return request({ method: 'GET', url: stepUpUrl });
}
